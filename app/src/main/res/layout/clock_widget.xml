<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
>
    <ImageView
        android:contentDescription="@string/clock_widget_image_description"
        android:id="@+id/clock_widget_image"
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:src="@drawable/clock_widget_image"
    />

    <TextView
        android:contentDescription="@string/clock_widget_text_description"
        android:id="@+id/clock_widget_text"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
    />
</RelativeLayout>
